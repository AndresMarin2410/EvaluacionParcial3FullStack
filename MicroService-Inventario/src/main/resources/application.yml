spring:
  application:
    name: MicroService-Inventario

  datasource:
    url: jdbc:mysql://localhost:3306/db_inventario
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  flyway:
    enabled: true

server:
  port: 8084

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /inventario/swagger-ui.html